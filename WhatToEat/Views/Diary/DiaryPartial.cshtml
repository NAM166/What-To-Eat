@model WhatToEat.Models.ViewModels.Diary.DiaryVM

    <div class="ajaxcart" style="color:white;">
        Your cart: @Model.Quantity item(s) - $@Model.Calorie
        <br />
        @Html.ActionLink("Checkout", "Index")
    </div>